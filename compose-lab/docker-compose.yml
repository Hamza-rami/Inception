services:
  server:
    image: python:3.12-slim
    command: python -m http.server 8000
    volumes:
      - ./website:/website
    working_dir: /website

  client:
    image: debian:12
    command: sleep infinity
    environment:
      NAME: Hamza
      SCHOOL: "1337"
    volumes:
      - ./shared:/shared